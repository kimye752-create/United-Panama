# 🎯 파나마 1공정 — 세션 6 핸드오프

**생성일**: 2026-04-12
**세션**: 6 (코드 트랙 본격 진입 — seed_loaders 3종 완전체)
**범위**: load_market_intel 작성·적재 → load_macro 첫 실행 → seed_panama runner 작성 → 3순위 정비 완료
**다음 세션 시작점**: D2 크롤러 트랙 (pa_panamacompra.ts OCDS API 우선)

---

## 0. 다음 세션 Claude에게 (⭐ 필수 읽기)

### 0-1. 본질
- KITA 무역AX 1기 · 한국유나이티드제약 5조 · 파나마 의약품 진출 B2B SaaS
- 달강 = 파나마 전담 + 엔진⑥(AHP 매칭) + 리드 개발자
- Claude(설계·검수) / Gemini(사용자 자료 수집·검증 **보조**) / Cursor(코드 구현)
- 1공정 산출물 = 품목별 시장 분석 보고서
- **3대 사양서 + v1.6 보고서** (다음 세션 첨부 권장):
  - `ARCHITECTURE.md` (세션 6 `pa_source` 허용값 목록 갱신 — `who_paho` 추가)
  - `CURSOR_INSTRUCTIONS.md` (세션 4 최신화, 세션 6 기준 수정 불필요)
  - `src/config/product-dictionary.ts` (세션 4 제형 보강)
  - `파나마_1공정_크롤링로직_설계보고서_v1_6_full.docx` (세션 5 완성)
- **핸드오프 세션 5는 Cursor에도 이미 공유됨**

### 0-2. ⚠ 표현 통일 (세션 3~6 확정 — 절대 준수)
- **"JSON seed 사전 수집"** = 사용자가 사전에 자료 수집·정제하여 정적 자료 형태로 DB화 (예: JSON, PDF)
- **Gemini ≠ 능동 크롤러**. Gemini는 사용자의 자료 수집·검증 **보조 도구**
- **데이터 신뢰성** = AI 생성 능력 아님. **사용자의 사전 검증**에 기반
- 이전 버전 차이점·변경 이력은 설계서 본문에 포함 금지. 핸드오프에만 기록

### 0-3. ⚠ 작업 방식
- 짧고 빠른 판정. 달강은 의사결정 빠름 — 옵션 2~3개 주면 즉시 결정
- 코드 직접 작성 금지 → Cursor에 라우팅 (Claude 크레딧 절약)
- Cursor 보고 "X건 적재"는 반드시 `SELECT COUNT` 1회 후 신뢰
- 풀 리뉴얼이 필요한 문서는 부분 수정 대신 **편집 모드(외과 수술식 치환)** 선호
- 컨텍스트 80%+ 도달 시 🔔 자동 세션 전환 + 핸드오프 생성

### 0-4. 운영 룰 (세션 2~6 회고 누적)
- **Gemini 산출물 즉시 파일화** — 채팅에만 두지 말 것. `data/seed/panama/*.json`으로 박제
- **Cursor 보고 검증 룰** — "X건 적재" 보고는 반드시 `SELECT COUNT` 1회 후 신뢰
- **공정 간 데이터 인터페이스 명시** — 1공정 설계 시 3공정 입력 요구사항 사전 검토
- **단순 데이터 복붙은 사용자 직접 편집 우선** — Cursor는 코드·검증용
- **문자열 파싱 시 부정 키워드 우선 매칭** — "미포함" → "포함" 순서
- **대형 docx 수정은 편집 모드** — 50장급 문서 풀 재작성은 단일 세션 토큰 한도 초과
- **⭐ 세션 6 추가 — 리스크 격리 전략**: 첫 실행 로더는 통합 runner에서 호출하지 말고 단독 실행으로 원인 분리. runner는 작성만 해두고 clean slate 때 첫 사용
- **⭐ 세션 6 추가 — 경쟁사/파트너 경계는 유동적**: 현지 제조사를 AHP 후보에서 배제하지 말 것. 라이센싱 인/CDMO 파트너로 전환 가능성 존재 → `pa_notes`에 `[COMPETITOR]` 태그로 기록만 하고 후보군에는 포함

---

## 1. 세션 6 완료 현황

### 1-1. 1순위 ✅ load_market_intel.ts 작성·적재
- **파일**: `src/seed_loaders/load_market_intel.ts`
- **입력**: `data/seed/panama/round3_market_intel.json` (6대 영역)
- **출력**: panama 12건 + panama_distributors 4건
- **6대 영역 → 테이블 분기**:
  - `pricing_data` (1건, 전부 NULL → confidence 0.30) → panama
  - `competitors` (3건: Medipan/Rigar/GSK) → panama + `pa_notes` `[COMPETITOR]` 태그
  - `distributors` (4건: Feduro/Celmar/Haseth/Astur) → **panama_distributors**
  - `infrastructure` (4건: ION/Hospital del Niño/Hospital Nacional/San Fernando) → panama
  - `regulatory_costs` (1건, 3개 비용 단일 행 통합) → panama
  - `epidemiology` (3건: 위궤양/위암/고지혈증) → panama
- **target_market 파싱**: `mapSpanishTargetToMarket` — 부정 키워드 우선 매칭 ("Público y Privado" → both / "Privado" → private / "Público" → public)
- **결과**: Feduro/Celmar/Astur = both, Haseth = public

### 1-2. 2순위 ✅ load_macro 단독 실행 + seed_panama runner 작성
- **load_macro 첫 실행**: 7건 적재 (sites 7개 = 행 7개 평탄화)
  - `pa_price_local` NULL 유지 (GDP 등 큰 수치는 `data` JSONB 내부에만 존재) → DECIMAL(20,4) overflow 리스크 원천 차단
  - `pa_source` 값: `worldbank`, `ita`, `kotra`, `motie`, `pubmed`, `acodeco`, **`who_paho`** (신규)
  - `pa_source_type` 제거 (절대 원칙 #8 준수)
  - `--dry-run` 플래그 추가
- **seed_panama runner**: `scripts/runners/seed_panama.ts` 작성 완료, 실행은 하지 않음 (중복 INSERT 방지)
- **package.json**: `seed:panama` / `seed:panama:dry` 스크립트 추가
- **⚠ runner 주석**: 재실행 시 중복 INSERT 경고 명시 — clean slate 때만 사용

### 1-3. 3순위 ✅ 정비 3건
- **ebnf_schemas.ts**: `src/utils/ebnf_schemas.ts` 생성. 3종 EBNF 스키마 (PRODUCT / MACRO / TENDER) + `getEbnfSchema(kind)` 헬퍼 + `default: never` exhaustiveness 분기
- **translate.ts stub**: `src/utils/translate.ts` 생성. `translateToKorean` 호출 시 throw. D6 Phase B 진입 시 실구현 예정
- **핸드오프 아카이브 이동**: 세션 1/2/3 → `docs/handoffs/archive/`. 루트에는 세션 4/5/6만 잔존
- **CHANGELOG.md**: 세션 6 정비 내역 기록

### 1-4. 문서 정합성 수정
- **ARCHITECTURE.md**: `pa_source` 허용값 목록에 `who_paho` 추가. Supabase 실데이터와 100% 일치

### 1-5. Supabase 현황 (세션 6 기준)
- **panama**: 0 → 12 → **19건**
  - gemini_seed (round3): 12건
  - macro 7종 (worldbank/ita/kotra/motie/pubmed/acodeco/who_paho): 각 1건
- **panama_distributors**: 0 → **4건** (Feduro/Celmar/Haseth/Astur)
- **panama_eml**: 16건 (세션 3 적재분 유지)
- **합계**: **39건 seed 확보**
- **fob_estimated_usd NULL 위반**: 0건 ✅ (절대 원칙 #3)

### 1-6. 진행률 현황
- **1공정 기준**: 약 35~40% (세션 5 25~30%에서 코드 트랙 본격 진입으로 상승)
- **1·2·3공정 전체**: 약 13~15%
- **남은 일정**: 4/24까지 약 12일
- **세션 6 가치**: 파나마 seed 3종 로더 전부 실전 검증 완료. 다음부터는 OCDS API·정부 폼·민간 약국 크롤러 트랙만 남음

---

## 2. 절대 원칙 (세션 6 기준 11개, 변경 없음)

1. **9:1 아키텍처** — JSON seed 사전 수집 90% / web_search 10%
2. workflow_dispatch만, cron 금지
3. fob_estimated_usd 1공정 NULL 강제
4. 공통 6컬럼 변경 금지
5. any 금지, .ts/.tsx 분리
6. product UUID는 전 나라 공통, 변경 금지
7. MAX_LLM_CALLS_PER_RUN=3
8. **pa_source_type 컬럼 사용 금지** (세션 3 디버깅 결과)
9. **문자열 파싱 시 부정 키워드 우선 매칭** ("미포함" → "포함" / "y Privado" → "Privado" / "Público" 순서)
10. **데이터 신뢰성 근거는 사용자 사전 검증** (AI 생성 능력 아님)
11. **"X건 적재" 보고는 `SELECT COUNT` 1회 검증 후 신뢰**

### 9개 고정 UUID (영구, 변경 금지)
```
MACRO_PRODUCT_ID                     : ba6cf610-9d7c-4fb9-9506-eabd7a5457b8
하이드린 캡슐(Hydroxyurea)           : bdfc9883-6040-438a-8e7a-df01f1230682
실로스탄 CR정(Cilostazol)            : fcae4399-aa80-4318-ad55-89d6401c10a9
가스티인 CR정(Itopride)              : 24738c3b-3a5b-40a9-9e8e-889ec075b453
클란자 CR정(Aceclofenac)             : 2504d79b-c2ce-4660-9ea7-5576c8bb755f
라베맥스 정(Rabeprazole)             : 859e60f9-8544-43b3-a6a0-f6c7529847eb
에르도스테인 캡슐(Erdosteine)        : 014fd4d2-dc66-4fc1-8d4f-59695183387f
오메가-3 연질캡슐(Omega-3 ee)        : f88b87b8-c0ab-4f6e-ba34-e9330d1d4e18
레보틱스 시럽 90mg(Levodropropizine) : 895f49ae-6ce3-44a3-93bd-bb77e027ba59
```

### pa_source 허용값 (세션 6 갱신 — ARCHITECTURE.md 일치)
```
worldbank / panamacompra / arrocha / metroplus / minsa / css / 
acodeco / kotra / ita / motie / pubmed / who_paho / llm_search / gemini_seed
```

---

## 3. 다음 세션 즉시 작업 (우선순위 순)

### 🔴 1순위 — pa_panamacompra.ts 작성 (D2 크롤러 첫 진입)
- **파일**: `src/crawlers/preload/pa_panamacompra.ts`
- **대상**: PanamaCompra OCDS API (`https://ocdsv2dev.panamacompraencifras.gob.pa/api/v1/ocds/release`)
- **필터**: UNSPSC 51000000 (의약품 분류 코드)
- **8 INN 검색**: 스페인어 키워드 순차 시도 (product-dictionary.ts 참조)
- **적용 기법**:
  - ⑨ EBNF Constrained Extraction (TENDER_SCHEMA_EBNF 사용 — 단, Anthropic API는 EBNF 직접 미지원이므로 **JSON Schema + Tool use로 우회 구현** 예정)
  - ⑦ Polite Scraping (OCDS API 1~2 req/sec, 1.5~3초 랜덤 딜레이)
- **출력**: panama 테이블에 `market_segment='public'`, `pa_price_type='tender_award'`, `pa_source='panamacompra'` 적재
- **검증**: 적재 후 `SELECT COUNT(*) FROM panama WHERE pa_source='panamacompra'`

### 🟡 2순위 — 남은 D2 공공 크롤러 3종
- `src/crawlers/preload/pa_minsa.ts` — MINSA faddi (Session + POST + CSRF), ⑧ HTML Preprocessing 적용
- `src/crawlers/preload/pa_css.ts` — 사회보장기금 PDF 배치, `pdf_parser.ts` 활용
- `src/crawlers/preload/pa_acodeco.ts` — CABAMED 가격 통제 리스트
- `scripts/runners/preload_public.ts`
- `.github/workflows/pa_static_public.yml` (workflow_dispatch만)

### 🟢 3순위 — D4~D5 민간 크롤러
- `src/crawler/stealth_setup.ts` (⑦ Polite + ⑩ Resource Blocking 통합)
- `src/crawlers/preload/pa_arrocha.ts` (Cloudflare WAF + Residential Proxy)
- `src/crawlers/preload/pa_metroplus.ts` (Cloudflare Turnstile + 사용자 행동 모사)
- 10가지 기법 코어 파일들 (webwalker / wrapper_maintenance / sand / comem / xgrammar / atomic)

### 🟢 4순위 — D6 Phase B 실시간 보강
- `src/utils/source_whitelist.ts` (`.gob.pa`, `.gov`, `.edu`, `.who.int`, `paho.org`)
- `src/utils/llm_cost_guard.ts`
- `src/config/llm-trigger-rules.ts`
- `src/logic/freshness_checker.ts`
- `src/crawlers/base/LLMSearchCrawler.ts` (Anthropic web_search)
- `src/utils/translate.ts` 실구현 (현재 stub)

---

## 4. 미결 사항

### 다음 세션 첫 작업 전 필요
- Anthropic API 키 발급 (D6 Phase B 전) — $5
- OCDS API 실제 엔드포인트 접근성 확인 (CORS / rate limit 정책)

### 킥오프 미팅 대기
- FOB Logic A/B 수식 확정
- Residential Proxy 계정 (Arrocha/Metro Plus 크롤링용)
- 운임 데이터 (2공정 엔진⑤용)
- AHP 가중치 (3공정 엔진⑥용)

### 장기
- ITA URL 변경 대응 (404 이슈)
- KOTRA 셀렉터 TODO
- MINSA essential 리스트 수집 (현재 panama_eml에 0건)
- EBNF → JSON Schema 변환 로직 (Anthropic Tool use 연동 시)

---

## 5. 파일 구조 스냅샷 (세션 6 기준)

```
루트/
  ├ 핸드오프_세션4.md
  ├ 핸드오프_세션5.md
  ├ 핸드오프_세션6.md ★세션6 생성
  ├ ARCHITECTURE.md (세션6 pa_source 목록 갱신)
  ├ CURSOR_INSTRUCTIONS.md
  ├ CHANGELOG.md (세션6 정비 내역 추가)
  ├ 파나마_1공정_크롤링로직_설계보고서_v1_6_full.docx
  └ docs/handoffs/archive/
     ├ 핸드오프_세션1.md ★세션6 이동
     ├ 핸드오프_세션2.md ★세션6 이동
     └ 핸드오프_세션3.md ★세션6 이동

data/seed/panama/
  ├ round1_macro.json (sites 7개) ← load_macro 실행 완료
  ├ round2_eml.json (8 INN)        ← load_eml 실행 완료
  └ round3_market_intel.json (6대 영역) ← load_market_intel 실행 완료

scripts/
  ├ ddl/
  │  ├ panama_table.sql
  │  ├ panama_add_pa_notes.sql
  │  ├ panama_distributors.sql
  │  └ panama_eml.sql
  ├ runners/
  │  ├ preload_macro.ts
  │  └ seed_panama.ts ★세션6 작성 (실행 안 함)
  └ verify_macro_seed_sql.ts ★세션6 생성 (검증용)

src/
  ├ config/
  │  └ product-dictionary.ts (세션4 제형 보강)
  ├ seed_loaders/
  │  ├ load_macro.ts ★세션6 실행 (7건 적재)
  │  ├ load_eml.ts (세션3 완성, 16건 적재)
  │  └ load_market_intel.ts ★세션6 작성·실행 (12+4건 적재)
  ├ crawlers/
  │  ├ base/ (D2~D6 채워질 예정)
  │  ├ preload/ (D2~D5 채워질 예정)
  │  └ realtime/ (D6 채워질 예정)
  └ utils/
     ├ db_connector.ts (세션2 완성)
     ├ pdf_parser.ts (세션2 완성)
     ├ html_preprocessor.ts (세션2 완성, 76.3% 토큰 절감 실측)
     ├ ebnf_schemas.ts ★세션6 생성 (3종 EBNF + 헬퍼)
     └ translate.ts ★세션6 stub 생성 (D6 실구현 예정)
```

### Supabase 현재 상태 (세션 6 기준)
- **panama**: 19건 (gemini_seed 12 + macro 7)
- **panama_distributors**: 4건 (Feduro/Celmar/Haseth/Astur)
- **panama_eml**: 16건 (WHO EML 8 + PAHO 8)
- **합계**: 39건

---

## 6. 골든 데이터 (세션 2~6 변경 없음)

- GDP $86.52B / 인구 4,515,577 / 1인당 $19,161 / 기대수명 80
- 의료비 1인당 $1,547 / GDP 8% / 시장성장률 10%/3년
- ★ 한국 위생선진국 지정 2023.6.28 (MINSA)
- 한-중미 FTA 발효 2021.3.1 (관세 0%)
- ITBMS 의약품 0% 면세
- 약국 마진 33%, 도매 23~25%
- 등록 비용: 신규 $500, 자유판매 $200, 갱신 $750, 처리 0.5~1개월
- WHO EML 2023: Hidroxiurea만 등재
- PAHO Strategic Fund: Hidroxiurea만 조달
- 경쟁사: Medipan, Rigar, GSK (panama 테이블 `pa_notes` `[COMPETITOR]` 태그로 적재 완료)
- 유통사 4개: Feduro, Celmar, Haseth, Astur (panama_distributors 적재 완료)
- 병원: ION, Hospital del Niño, Hospital Nacional, San Fernando (panama 테이블 적재 완료)

---

## 7. 다음 세션 첫 명령 (복붙용)

```
@핸드오프_세션6.md
@ARCHITECTURE.md
@CURSOR_INSTRUCTIONS.md
@src/config/product-dictionary.ts
@src/utils/ebnf_schemas.ts

세션 7 시작. 세션 6에서 seed_loaders 3종 완전체 완료됨.
panama 19건 / panama_distributors 4건 / panama_eml 16건 = 총 39건 seed 확보.
이제 D2 공공 크롤러 트랙 본격 진입.

1순위: src/crawlers/preload/pa_panamacompra.ts Cursor 라우팅
- 대상: PanamaCompra OCDS API (https://ocdsv2dev.panamacompraencifras.gob.pa/api/v1/ocds/release)
- 필터: UNSPSC 51000000 (의약품)
- 검색: 8 INN 스페인어 키워드 순차 (product-dictionary.ts)
- 기법: ⑨ EBNF → JSON Schema 변환 후 Anthropic Tool use 연동 / ⑦ Polite Scraping
- 출력: panama 테이블 market_segment='public', pa_source='panamacompra'
- 검증: SELECT COUNT 1회 후 완료 보고

2순위: pa_minsa.ts / pa_css.ts / pa_acodeco.ts 순차
3순위: preload_public runner + workflow_dispatch 배포
4순위: D4~D5 민간 크롤러 (stealth_setup + Arrocha/Metro Plus)
5순위: D6 Phase B 실시간 보강 (LLMSearchCrawler + translate 실구현)
```

---

## 8. 세션 6 회고 (짧게)

- **잘된 것**: 리스크 격리 전략(옵션 A) 선택으로 load_macro 첫 실행 이슈 없이 통과. `pa_price_local` NULL 유지 설계가 DECIMAL overflow 리스크 원천 차단. Cursor 보고 품질 우수 (자발적 CHANGELOG 기록, `never` exhaustiveness 분기 추가, 검증 스크립트 별도 생성).
- **판단 전환점**: 3순위 정비 작업을 "세션 6에 묶어야 할지 세션 7로 넘겨야 할지" 고민에서 옵션 A(묶어서 처리) 선택이 적중. ebnf_schemas.ts가 pa_panamacompra.ts의 전제 조건이므로 선작성이 다음 세션 진입 속도를 크게 단축시킴.
- **경쟁사 처리 철학**: 달강이 "협력사가 경쟁사가 될 수도 있고 경쟁사가 협력사가 될 수도 있다"고 지적. AHP에서 경쟁사 배제가 아닌 차등 평가 원칙 재확인. 이 판단이 3공정 AHP 설계 시에도 유효하게 적용될 것.
- **학습**: (1) 첫 실행 로더는 통합 runner가 아닌 단독 실행으로 리스크 격리. (2) EBNF는 Anthropic에서 직접 지원하지 않으므로 JSON Schema + Tool use 우회 구현 예정. 세션 7 pa_panamacompra.ts 작성 시 실제 변환 필요. (3) 대형 docx 편집 모드 + 코드 트랙 단독 실행 검증이 세션 6을 안정적으로 끝낸 2대 축.
- **경계 사항**: Cursor 보고 숫자는 반드시 SQL 재검증. 세션 6에서도 fob_null_violations만 SQL Editor로 직접 확인했는데, 이게 절대 원칙 #11의 핵심 실천. 앞으로도 유지.

---

**세션 6 종료. seed_loaders 3종 완전체. Supabase 39건 확보. 세션 7부터 D2 크롤러 트랙 본격 진입.**

---

## 9. 세션 7 W1 미결 (핸드오프 전용)

- **MINSA (`pa_minsa.ts`)**: 비 `--dry-run` 실행 시 `MINSA: CSRF session required, manual cookie injection planned in session 8` 고정 throw. 세션 8에서 수동 쿠키·CSRF 토큰 주입 또는 Playwright 세션 재사용으로 Full 전환 예정. `--dry-run`은 mock 2건·DB 미적재(`BaseCrawler` dry-run 분기).