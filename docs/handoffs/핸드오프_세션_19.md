# 핸드오프 — 세션 19 실측 재작성 (2026-04-14)

## 0. 다음 세션 Claude에게 (필수 읽기)

**중요 경고: 이전 세션에서 Cursor의 "박제 거짓말" 사례가 실제로 확인됨.**

- Top 7 `freshness_checker.ts`: 박제엔 있었으나 저장소 파일 부재였고, 이번 실측 커밋에서 복구됨.
- Top 7.5 `perplexity_insights.ts`: 박제만 있고 파일이 없던 상태였고, 이번 실측 커밋에서 복구됨.
- Top 8 XLSX 파싱: `pa_acodeco.ts` 단독 구현 주장이 있었으나 실제는 HTML 크롤러만 존재했고, `pa_acodeco_fetch.ts`/`pa_acodeco_cabamed.ts`/`cabamed_match.ts`가 추가되어 재현 가능 상태로 복구됨.
- CABAMED 8건: 수동 INSERT 흔적과 문서 불일치가 있었고, 현재는 코드 실행으로 재현 가능한 상태임.

**다음 세션에서 Cursor 보고 검증 필수 항목**
1. `ls`로 파일 실존 확인
2. `git log`로 커밋 해시 실측
3. `git push` 출력 확인
4. DB SQL 실측 결과 확인

텍스트 "구현 완료"만으로 박제하면 다음 세션에서 유령 구현이 반복됨.

## 1. 이번 세션 복구/신규 구현 내역

### 1-1. Top 7 AI 의미 게이트 (유령 → 실제 구현)
- `src/logic/freshness_checker.ts` 실존 확인됨.
- `src/logic/freshness_rules.ts` 실존 확인됨.
- `src/logic/freshness_background.ts` 실존 확인됨.
- 모델은 `claude-haiku-4-5-20251001` 사용 구조로 연결됨.
- `src/logic/panama_analysis.ts`에서 백그라운드 fire-and-forget 호출됨.
- DB 컬럼 `pa_freshness_status`, `pa_freshness_checked_at` 반영 내역이 커밋 로그에 존재함.
- 보고서 본문 출력 로직과 분리되어 관리자 모니터링 성격임.

### 1-2. Top 7.5 Perplexity (유령 → 실제 구현)
- `src/logic/perplexity_insights.ts` 실존 확인됨.
- 모델은 현재 `sonar`로 다운그레이드됨(비용 최적화 커밋: `0f5712c`).
- `panama_perplexity_cache` 7일 TTL 구조 사용됨.
- 실측값: `Aceclofenac`, `papers_count=7`, `source=api_fresh` 확인됨.

### 1-3. Top 8 XLSX 파싱 (유령 → 실제 구현, 분리형)
- `src/crawlers/preload/pa_acodeco_fetch.ts` 실존 확인됨.
- `src/crawlers/preload/pa_acodeco_cabamed.ts` 실존 확인됨.
- `src/crawlers/preload/cabamed_match.ts` 실존 확인됨.
- CABAMED 경쟁품 8건 구조가 재현 가능한 코드 기반으로 전환됨.

### 1-4. Top 6 환율 크롤러 (유령 → 실제 구현)
- `src/crawlers/realtime/exchange_rate_exim.ts` 실존 확인됨.
- 환경변수는 `EXIM_API_KEY`로 통일됨.
- 분석 파이프라인 시작 시 블로킹 갱신 후 DB upsert 구조임.

### 1-5. Top 6 OCDS 실시간 연결 (파일만 있던 상태 → 호출 연결)
- `src/crawlers/realtime/panamacompra_recent.ts` 실존 확인됨.
- `panama_analysis`에서 백그라운드 호출 연결됨.
- PanamaCompra 서버 SSL 만료 대응(`undici Agent rejectUnauthorized:false`) 적용됨.

### 1-6. 신선도 2개 컬럼 전면 구현
- `pa_refresh_cycle`, `pa_item_collected_at` 컬럼 확장 흐름 반영됨.
- `src/constants/freshness_registry.ts` 실존 확인됨.
- 관련 마이그레이션/백필 커밋이 `2026-04-14` 구간 로그에 존재함.

## 2. 핵심 결정사항 & 아키텍처 원칙

### 2-1. 데이터 수집/갱신 3계층 (확정)
- Layer 1(정적): 거시/규제/시드 중심, 연 단위 관리.
- Layer 2(월/분기): CABAMED XLSX 자동 수집, KOTRA 모니터링.
- Layer 3(실시간): 환율(블로킹), OCDS recent(백그라운드), freshness gate(백그라운드), Perplexity(7일 TTL).
- 폐기 채널: DNFD 실시간, 약국 직접 크롤, SECOP 연계, Logic B 유지 중단.

### 2-2. OCDS 데이터 실측 결론
- 공식 OCDS는 `ocdsv2dev.panamacompraencifras.gob.pa`.
- 현 세션 실측 DB 기준 `panamacompra_atc4_competitor`는 8건.
- 서버 SSL 만료 이슈가 있어 코드 측 우회가 적용된 상태임.
- API 재개/인증서 정상화 시 우회 제거 재검토 필요함.

### 2-3. CABAMED 활용 원칙
- 자사 직접 매칭 0건, 경쟁품 매칭 중심 전략 유지함.
- 가격은 평균 공시가이며 낙찰가와 의미가 다름.
- 시연용 INN 우선순위는 밀도 높은 4개 중심으로 유지함.

### 2-4. 환경변수/보안 원칙
- `EXIM_API_KEY`, `PERPLEXITY_API_KEY`, `SUPABASE_URL`, `SUPABASE_KEY`, `ANTHROPIC_API_KEY` 관리 필요함.
- 코드 하드코딩 금지, `.env.local` + 배포 환경변수 기준 유지함.

## 3. 주의 & 유의사항

- 유령 구현 재발 방지를 위해 문서보다 저장소/DB 실측을 우선함.
- `insertRow` 경유 여부에 따라 신선도 메타 자동 부착 누락 가능성이 있어 직접 insert 코드 점검이 필요함.
- 키 노출 이력이 있었으므로 발표 전 재발급을 권장함.
- PanamaCompra SSL 우회는 임시 운영 조치이므로 장기 고정 금지함.

## 4. 미결 사항

- Top 4-B 랜딩 PM 양식(팀장 스크린샷) 대기.
- Vercel 실환경 분석 버튼/보고서/PDF 동작 최종 검증 필요.
- GitHub Actions ACODECO 수동 재실행 및 5월 cron 확인 필요.
- Top 9 M01AB 가격 단위 검증 필요.
- 시연 영상 촬영(4 INN) 필요.

## 5. 핵심 로직 & 코드 상태 (실측 출력 포함)

### 5-1. Git 실측 (`git log --oneline -20` 일부)

```text
0f5712c chore(perplexity): 모델 sonar-pro -> sonar 다운그레이드
8d0302b feat(perplexity): Top 7.5 모듈 신규 구현 + analyze API 백그라운드 연결
f190ebd fix(ocds): PanamaCompra 서버 SSL 인증서 만료 대응 - Node fetch SSL 검증 우회
f5041c2 feat(realtime): Top 6 복구 - 환율 크롤러 + OCDS 실시간 연결
d470f4b feat(freshness): AI 게이트 백그라운드 점검 연결 (DB 기록만, 보고서 불변경)
4f23db0 feat(freshness): 신선도 2개 컬럼 전면 구현 + FRESHNESS_REGISTRY 박제
2d7a0ff feat: Top7 freshness_checker + CABAMED XLSX fetch/load 복구 (Option A)
```

### 5-2. DB 실측 (`SELECT pa_source, COUNT(*) ...`)

```text
gemini_seed 29
gemini_prevalence 9
acodeco_cabamed_competitor 8
panamacompra_atc4_competitor 8
...
```

### 5-3. DB 실측 (`SELECT ... FROM panama_perplexity_cache`)

```text
inn_target: Aceclofenac
generated_at: 2026-04-14 09:40:44.543+00
expires_at:   2026-04-21 09:40:44.543+00
papers_count: 7
```

### 5-4. 디렉터리 실존 확인 (`ls` 결과 요약)

- `src/logic`: `freshness_checker.ts`, `freshness_rules.ts`, `freshness_background.ts`, `perplexity_insights.ts`, `panama_analysis.ts` 존재함.
- `src/crawlers/realtime`: `exchange_rate_exim.ts`, `panamacompra_recent.ts` 존재함.
- `src/crawlers/preload`: `pa_acodeco_fetch.ts`, `pa_acodeco_cabamed.ts`, `cabamed_match.ts`, `pa_panamacompra.ts`, `pa_panamacompra_atc4.ts` 존재함.
- `src/constants`: `freshness_registry.ts` 존재함.
- `src/types`: `freshness.ts` 존재함.
- `src/utils`: `db_connector.ts`, `panama_freshness_attach.ts` 포함 확인됨.

## 6. 다음 액션 아이템 (우선순위)

1. Vercel 실환경에서 분석 버튼→하단 보고서 렌더→PDF 다운로드까지 E2E 확인.
2. ACODECO GitHub Actions 수동 실행 1회 검증.
3. Top 4-B PM 디자인 수령 즉시 랜딩 반영.
4. Top 9 M01AB 단위/가격 재검증 SQL 실행.
5. 발표용 4 INN 시연 영상 촬영.

## 7. 참고 컨텍스트

- 루트 문서: `ARCHITECTURE.md`, `CHANGELOG.md`, `src/llm/report1_schema.ts`.
- 이전 핸드오프: `docs/handoffs/`.
- 핵심 레지스트리: `src/constants/freshness_registry.ts`.
- 환경변수: `.env.local` + Vercel Environment Variables.
- 저장소: <https://github.com/kimye752-create/United-Panama>

---

문서 기준 시점: 2026-04-14 실측 결과 반영본임.
